###instalar linux
usar llaves de Git - dnysecure 
- las llaves ya estan en .ssh de windows fueron creadas con powershell - ssh-keygen 
    ssh-keygen -t ed25519 -C "dnydatos@gmail.com" ---- clave: Hymalaya2010 de llave
conectarse por powershell: ssh prueba@192.168.1.210 --- clave:Prueba123

--configurar red en ubuntu 
 
 ls /etc/netplan - para ubicar el archivo de configuración de red (ej. 50-cloud-init.yaml, 01-netcfg.yaml)
 sudo nano /etc/netplan/50-cloud-init.yaml.

 Ejemplo completo con la IP y DNS solicitados (ajusta el nombre de interfaz en ethernets:):


network:
  version: 2
  ethernets:
    ens33:
      dhcp4: false
      dhcp6: false
      addresses:
        - 192.168.1.210/24
      routes:
        - to: default
          via: 192.168.1.1
      nameservers:
        addresses:
          - 200.21.200.80
          - 200.21.200.10

guardamos cambios

aplicamos cambios en netplan
sudo netplan apply - aplicamos cambios
ip add - revisamos que se tengan los cambios

opcional:
reiniciamos red -  sudo systemctl restart NetworkManager --
si no sale debes hacer lo siguiente:

systemctl is-active systemd-networkd - verifica servicios activos
systemctl is-active NetworkManager

si esta activo systemd-networkd 
sudo systemctl restart systemd-networkd